{
  "name": "alivio-vital-home-care",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:vercel": "next build",
    "build:with-images": "npm run optimize:images && next build",
    "optimize:images": "node scripts/optimize-images.js"
  }
}
